// problem statement:
/* 1. Given a list of words.
   2. Given M x N board where every cell has one character.
    Find all possibke words that are part of the list and can be formed
    ny a sequence of adjacent characters from board.
    Note that we can move to any of 8 adjacent characters, but a 
    word should not have multiple instances of same cell.
*/

#include<bits/stdc++.h>
#define endl "\n"
using namespace std;
#define mod 1000000007
#define ll long long

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    return 0;
}